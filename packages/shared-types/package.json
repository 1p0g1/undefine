{
  "name": "@undefine/shared-types",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "clean": "rm -rf dist",
    "prebuild": "tsc --noEmit",
    "build": "npm run clean && tsc -p tsconfig.prod.json && npm run verify:build",
    "verify:build": "[ -f dist/index.d.ts ] || (echo '❌ Types build failed!' && exit 1)",
    "typecheck": "tsc --noEmit",
    "test": "echo 'No tests yet'"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
